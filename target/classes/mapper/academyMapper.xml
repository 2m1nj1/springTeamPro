<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="academyMapper">

	<insert id="userInsert" parameterType="LogVO">
		INSERT INTO TEAM2(user_Id, user_Pass, user_Name )
		VALUES(#{user_Id}, #{user_Pass}, #{user_Name})
	</insert>
	
	<select id="idCheck" parameterType="LogVO" resultType="LogVO">
		SELECT * FROM TEAM2
		WHERE	user_Id = #{user_Id}
			<if test="user_Pass != null">
			AND user_Pass = #{user_Pass}
			</if>
	</select>
	
</mapper>
